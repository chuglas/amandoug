<h1>Profile page</h1>
<h1><%= user.username %></h1>
<ul>
  <% user.userEvents.forEach((oneEvent) => { %>
    <li>
      <h3> <a href='/events/<%= oneEvent._id %>'><%= oneEvent.name %> </a></h3>
    </li>
  <% }) %>
</ul>
<% if (user) { %>
  <div>
    <a href="/">Index</a>
  </div>
<% } %>
